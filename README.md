# Student Diary (Android)

Мобільний додаток для студента — щоденник, який поєднує:
- розклад занять
- менеджер навчальних завдань
- контроль успішності
- зберігання навчальних матеріалів

Проєкт розробляється як навчальний командний проєкт з фокусом на
чисту архітектуру та сучасний Android-стек.

---

## 🧱 Архітектура

Проєкт побудований за принципами **Clean Architecture + MVVM**.

Основні шари:

- domain – бізнес-логіка та моделі
- data – робота з базою даних (Room)
- presentation – ViewModel, UiState, Events
- ui – Jetpack Compose UI


### Основні принципи:
- Domain не залежить від Android
- Presentation працює тільки через UseCase
- UI не звертається напряму до data або domain
- Уся взаємодія UI → ViewModel відбувається через Events

---

## 📦 Технологічний стек

- **Kotlin**
- **Jetpack Compose**
- **Room** (offline-first)
- **Coroutines / Flow**
- **MVVM**
- Git (feature-based workflow)

---

## 🧩 Поточний стан розробки

### Реалізовано:
- Domain-рівень для модуля Tasks
- UseCase-и для роботи із завданнями
- Presentation-логіка:
    - UiState
    - Events
    - ViewModel
- Data-рівень (Room) для Tasks (в процесі інтеграції)
- Git-структура з feature-гілками

### В роботі:
- UI (Compose screens)
- Інтеграція data → presentation
- Навігація між екранами

---

## 👥 Командна робота

### Домовленості:
- **Domain**: не змінюється без узгодження
- **Data**: працює тільки в `data/`
- **UI**:
    - екрани в `presentation/screen`
    - ViewModel в `presentation/viewmodel`
- Заборонено напряму звертатись з UI до repository

### Git workflow:
- `main` — стабільна гілка
- `feature/*` — робота над окремими модулями
- Злиття через перегляд змін

---

## ▶️ Запуск проєкту

1. Відкрити проєкт в Android Studio
2. Дочекатись Gradle Sync
3. Запустити на емуляторі або фізичному пристрої

> На ранньому етапі можливий запуск без UI — для перевірки архітектури.

---

## 📌 Примітки

Проєкт розробляється в навчальних цілях з акцентом на правильну
архітектуру, а не на швидку реалізацію UI.
